# Copy to .env on the server and fill in values.
# docker-compose.yml reads these via ${VAR:-} syntax.

PORT=3001
NOINDEX=false

# Telegram notifications for new leads (optional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
